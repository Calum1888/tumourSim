% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/POWER.R
\name{simulate_metrics_all}
\alias{simulate_metrics_all}
\title{Monte Carlo coverage and width comparison: KM vs copula PFS}
\usage{
simulate_metrics_all(
  M = 1000,
  B = 200,
  n_patients = 150,
  mean,
  covariance,
  threshold,
  alpha_lesion = -2.5,
  beta_lesion = 0,
  gamma_lesion = 0.2,
  treatment_arm = 0,
  grid = 1:5,
  families = c("clayton", "frank", "gumbel"),
  true_S = NULL,
  verbose = TRUE
)
}
\arguments{
\item{M}{Integer. Number of replications. Defaults to 1000.}

\item{B}{Integer. Bootstrap replicates per copula fit. Defaults to 200.}

\item{n_patients}{Integer. Patients per simulated trial. Defaults to 150.}

\item{mean, covariance, threshold}{Tumour-model parameters.}

\item{alpha_lesion, beta_lesion, gamma_lesion}{Lesion-model parameters.}

\item{treatment_arm}{Numeric (typically 0/1).}

\item{grid}{Numeric vector of evaluation visits. Defaults to \code{1:5}.}

\item{families}{Character vector of copula families. Defaults to
\code{c("clayton", "frank", "gumbel")}.}

\item{true_S}{Optional numeric vector of true PFS values on
\code{grid}. If \code{NULL}, computed by \code{\link{true_pfs}}.}

\item{verbose}{Logical. Defaults to \code{TRUE}.}
}
\value{
A list with elements \code{summary} (long-format
  \code{data.frame}) and \code{raw} (per-replication matrices and
  per-family lists).
}
\description{
Single-arm Monte Carlo study comparing the direct Kaplan-Meier
estimator of PFS on the composite endpoint against the copula-based
estimator from \code{\link{pfs_copula_boot}} across one or more
copula families.
}

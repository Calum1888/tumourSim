% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AUGBIN.R
\name{S_lesion_from_params}
\alias{S_lesion_from_params}
\title{Lesion-side survival from logistic-regression parameters}
\usage{
S_lesion_from_params(beta, fit, lesion_out, treatment)
}
\arguments{
\item{beta}{Numeric vector of logistic-regression coefficients.}

\item{fit}{A fitted \code{\link[stats]{glm}} object (used only to
reconstruct the design matrix via \code{model.matrix} on its
formula).}

\item{lesion_out}{A list with components \code{events} (lesion event
matrix) and \code{all_tumour_sizes} (per-visit tumour sizes,
including baseline), e.g. from \code{\link{lesion_events}}.}

\item{treatment}{Numeric vector of length \code{nrow(events)} giving
the patient-level treatment indicator.}
}
\value{
Numeric vector of length \code{T} giving \eqn{S_D(t)} for
  \eqn{t = 1, \ldots, T}.
}
\description{
Computes \eqn{S_D(t)}, the probability of no new lesion by visit
\eqn{t}, given fitted logistic-regression coefficients \code{beta} and
the model design implied by \code{fit}. The per-visit non-event
probability is averaged across patients to obtain a population-level
survival via cumulative product.
}

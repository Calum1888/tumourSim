% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/POWER.R
\name{run_copula_power}
\alias{run_copula_power}
\title{Power study driver for the copula-based two-arm Wald test}
\usage{
run_copula_power(
  scenarios,
  defaults,
  M = 1000,
  B = 200,
  n_per_arm = 150,
  alpha_level = 0.05,
  test_visit = 5,
  family = "gumbel",
  grid = 1:5,
  base_seed = 1,
  n_cores = NULL,
  verbose = TRUE
)
}
\arguments{
\item{scenarios}{Named list of scenario lists.}

\item{defaults}{Defaults list passed through to
\code{\link{one_copula_rep}}.}

\item{M}{Integer. Number of replications. Defaults to 1000.}

\item{B}{Integer. Bootstrap replicates per copula fit. Defaults to 200.}

\item{n_per_arm}{Integer. Patients per arm. Defaults to 150.}

\item{alpha_level}{Numeric. Rejection threshold. Defaults to 0.05.}

\item{test_visit}{Integer. Visit at which to perform the Wald test.
Defaults to 5.}

\item{family}{Character copula family. Defaults to \code{"gumbel"}.}

\item{grid}{Numeric vector of evaluation visits. Defaults to \code{1:5}.}

\item{base_seed}{Integer.}

\item{n_cores}{Integer. Number of parallel workers. \code{NULL} uses
\code{detectCores() - 1}.}

\item{verbose}{Logical.}
}
\value{
A \code{data.frame} with columns \code{scenario},
  \code{rejection}, \code{mc_se}, \code{n_valid}.
}
\description{
Runs a Monte Carlo power study across scenarios using the two-arm
Wald test on the difference Delta = S_treat - S_control at a chosen
visit, with bootstrap-based per-arm standard errors from the copula
model.
}
\details{
For scenarios with administrative censoring before
  \code{test_visit}, the test is performed at \code{min(test_visit, cens_at)}.
}

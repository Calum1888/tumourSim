% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/POWER.R
\name{evaluate_augbin_parallel}
\alias{evaluate_augbin_parallel}
\title{Parallel Monte Carlo evaluator for the AugBin PFS estimator}
\usage{
evaluate_augbin_parallel(
  M = 1000,
  B = 200,
  n_patients,
  mean_vec,
  cov_mat,
  S_true,
  threshold = 1.2,
  alpha_lesion = -2.5,
  beta_lesion = 0,
  gamma_lesion = 0.2,
  treatment_arm = 0,
  base_seed = 1,
  n_cores = NULL,
  verbose = TRUE
)
}
\arguments{
\item{M}{Integer. Number of replications. Defaults to 1000.}

\item{B}{Integer. Bootstrap replicates per replication. Defaults to 200.}

\item{n_patients}{Integer.}

\item{mean_vec}{Numeric vector of length \code{T} (tumour mean).}

\item{cov_mat}{Numeric \code{T} x \code{T} covariance matrix.}

\item{S_true}{Numeric vector of true PFS values at visits \code{1:T},
used to evaluate coverage and bias.}

\item{threshold}{Numeric. Tumour progression threshold. Defaults to 1.2.}

\item{alpha_lesion, beta_lesion, gamma_lesion, treatment_arm}{Lesion-model
parameters.}

\item{base_seed}{Integer.}

\item{n_cores}{Integer or \code{NULL}.}

\item{verbose}{Logical.}
}
\value{
A list with elements \code{estimate}, \code{bias},
  \code{coverage_rate}, \code{avg_ci_width},
  \code{mc_se_estimate}, \code{mc_se_coverage}, \code{mc_se_width},
  \code{n_failed}.
}
\description{
Single-arm Monte Carlo evaluator for the AugBin estimator. Each
replication is seeded for reproducibility regardless of platform or
core count. Returns mean point estimate, bias, coverage rate, mean CI
width, and Monte Carlo standard errors for each.
}

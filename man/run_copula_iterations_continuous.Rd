% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Copula_Approach.R
\name{run_copula_iterations_continuous}
\alias{run_copula_iterations_continuous}
\title{Run continuous-time bootstrap copula PFS estimation across simulation iterations}
\usage{
run_copula_iterations_continuous(
  n_times,
  n_patients,
  n_iterations,
  mean,
  covariance,
  alpha,
  beta,
  gamma,
  R,
  copula_family,
  B = 100,
  threshold = 1.2
)
}
\arguments{
\item{n_times}{Integer. Number of equally spaced continuous time points at
which to evaluate the PFS curve in each iteration.}

\item{n_patients}{Integer. Number of patients per simulated dataset.}

\item{n_iterations}{Integer. Number of outer simulation iterations.}

\item{mean}{Numeric vector of length \code{n_times}. Mean vector for the
multivariate normal distribution of log tumour-size ratios.}

\item{covariance}{Numeric matrix (\code{n_times x n_times}). Covariance
matrix for the multivariate normal distribution.}

\item{alpha}{Numeric scalar. Intercept for the logistic new-lesion model.}

\item{beta}{Numeric scalar. Treatment-effect parameter.}

\item{gamma}{Numeric scalar. Tumour-size effect parameter.}

\item{R}{Numeric scalar or vector. Treatment indicator(s).}

\item{copula_family}{Integer. Copula family passed to
\code{VineCopula::BiCopEst()}.}

\item{B}{Integer. Number of bootstrap resamples per iteration. Default 100.}

\item{threshold}{Numeric. Tumour growth threshold for progression. Default 1.2.}
}
\value{
A data frame with \code{n_times} rows and three columns:
  \describe{
    \item{Time}{The continuous time grid point.}
    \item{Rate}{Mean estimated PFS at each time point across iterations.}
    \item{CI_Width}{Mean bootstrap CI width at each time point.}
    \item{Coverage}{Proportion of CIs containing the true rate at each
      time point.}
  }
}
\description{
A continuous-time replacement for \code{run_copula_iterations()}. Repeatedly
generates fresh datasets from the RECIST simulation model and applies
\code{bootstrap_copula_pfs_continuous()} to each, returning pointwise
coverage, mean CI width, and mean survival estimate across all iterations.
}
\details{
The true PFS rates are computed by \code{get_true_rates()} using 100,000
patients. Because the continuous time grid is defined within each iteration
from the simulated data's \code{max_t}, it can vary slightly between
iterations. To ensure the true rates and estimated curves are comparable,
the true rates are also evaluated on the same fixed integer grid
\code{1:n_times} (matching the discrete event times) and then coverage is
assessed pointwise.

Parallelisation is controlled by the caller via the \code{future} backend.
Call \code{future::plan(multisession)} before running this function to
enable parallel computation. If no plan is set, execution falls back to
sequential.
}
\seealso{
\code{run_copula_iterations} for the discrete-time equivalent,
  \code{bootstrap_copula_pfs_continuous} for the inner bootstrap function.
}

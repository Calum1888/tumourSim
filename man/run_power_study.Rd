% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/POWER.R
\name{run_power_study}
\alias{run_power_study}
\title{Power study driver for the KM log-rank test}
\usage{
run_power_study(
  scenarios,
  defaults,
  M = 1000,
  n_per_arm = 150,
  alpha_level = 0.05,
  verbose = TRUE
)
}
\arguments{
\item{scenarios}{A named list of scenario lists. Each scenario may
override \code{mean_vec}, \code{covariance}, \code{alpha},
\code{beta}, \code{gamma}, \code{cens_at}; must contain \code{beta}
and a human-readable \code{label}.}

\item{defaults}{A list of default DGP parameters (see
\code{\link{simulate_one_trial}}).}

\item{M}{Integer. Number of replications per scenario. Defaults to 1000.}

\item{n_per_arm}{Integer. Patients per arm. Defaults to 150.}

\item{alpha_level}{Numeric in \eqn{(0, 1)}. Rejection threshold.
Defaults to 0.05.}

\item{verbose}{Logical. Print progress messages. Defaults to \code{TRUE}.}
}
\value{
A \code{data.frame} with one row per scenario and columns
  \code{scenario}, \code{rejection}, \code{mc_se}, \code{n_valid}.
}
\description{
Runs a Monte Carlo power study across one or more scenarios using the
Kaplan-Meier log-rank test as the test statistic. For each scenario,
\code{M} two-arm trials are simulated via \code{\link{simulate_one_trial}}
and the empirical rejection rate is reported alongside a Monte Carlo
standard error.
}

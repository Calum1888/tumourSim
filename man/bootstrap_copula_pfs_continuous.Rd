% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Copula_Approach.R
\name{bootstrap_copula_pfs_continuous}
\alias{bootstrap_copula_pfs_continuous}
\title{Bootstrap progression-free survival estimates using continuous-time copula}
\usage{
bootstrap_copula_pfs_continuous(
  lesion_data,
  tumour_size_data,
  n_times,
  copula_family,
  B = 100,
  alpha_level = 0.05,
  true_pfs,
  threshold = 1.2,
  seed = NULL
)
}
\arguments{
\item{lesion_data}{A matrix where each row is a subject and each column a
follow-up time. Entries are binary (0/1); passed to \code{lesion_event()}.}

\item{tumour_size_data}{A matrix where each row is a subject and each column
a tumour size measurement. The first column is baseline; passed to
\code{tumour_event()}.}

\item{n_times}{Integer. Number of equally spaced time points at which to
evaluate the PFS curve. The grid runs from \code{max_t / n_times} to
\code{max_t}, where \code{max_t} is the maximum observed event time across
both endpoints in the full data.}

\item{copula_family}{Integer specifying the copula family passed to
\code{VineCopula::BiCopEst()}.
1 = Gaussian, 2 = t-Student, 3 = Clayton, 4 = Gumbel, 5 = Frank.}

\item{B}{Integer. Number of bootstrap resamples. Defaults to 100.}

\item{alpha_level}{Numeric. Significance level for the normal-approximation
CI. Defaults to 0.05, giving a 95% CI.}

\item{true_pfs}{A numeric vector of length \code{n_times} containing the
known true PFS probabilities at each time point, used to compute pointwise
coverage.}

\item{threshold}{Numeric. Multiplicative threshold for tumour progression,
passed to \code{tumour_event()}. Defaults to 1.2.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}
}
\value{
A list with the following elements:
  \describe{
    \item{time}{Numeric vector of length \code{n_times}. The fixed
      continuous time grid at which all curves are evaluated.}
    \item{boot_curves}{A matrix of dimension \code{B_valid x n_times} of
      bootstrap PFS estimates, one row per successful resample.}
    \item{ci_lower}{Numeric vector of length \code{n_times}. Lower bound of
      the pointwise normal-approximation CI.}
    \item{ci_upper}{Numeric vector of length \code{n_times}. Upper bound of
      the pointwise normal-approximation CI.}
    \item{coverage}{Integer vector of length \code{n_times}. Pointwise
      indicator of whether the true PFS falls within the CI.}
    \item{mean_coverage}{Scalar. Mean pointwise coverage.}
    \item{mean_ci_width}{Scalar. Mean pointwise CI width.}
  }
}
\description{
A continuous-time replacement for \code{bootstrap_copula_pfs()}. Fits the
copula once on the full data using pseudo-observations, then bootstraps only
the marginal Kaplan-Meier curves, evaluating each at a fixed continuous time
grid derived from the full data. This avoids the tied pseudo-observation
problem that arises when event times are discrete integers.
}
\details{
The time grid is fixed from the full observed data to ensure all bootstrap
curves are evaluated at identical time points, making it possible to stack
them into a matrix for pointwise CI computation. Without a fixed grid,
\code{copula_margin_estimation_continuous()} would derive a different grid
for each resample (since \code{max_t} varies), making aggregation impossible.
}
\seealso{
\code{bootstrap_copula_pfs} for the discrete-time equivalent,
  \code{copula_margin_estimation_continuous} for marginal estimation,
  \code{run_copula_iterations_continuous} for the outer simulation loop.
}

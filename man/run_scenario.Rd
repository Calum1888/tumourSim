% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/POWER.R
\name{run_scenario}
\alias{run_scenario}
\title{Three-method Monte Carlo study for a single scenario}
\usage{
run_scenario(
  M,
  B,
  n_per_arm,
  mean_ctrl,
  mean_trt,
  cov_mat,
  threshold = 1.2,
  alpha_lesion = -2.5,
  beta_lesion = 0,
  gamma_lesion = 0.2,
  gamma_lesion_ctrl = NULL,
  gamma_lesion_trt = NULL,
  tumour_dist = "mvnorm",
  tumour_df = 4,
  lesion_link = "logit",
  lesion_nonlinear = FALSE,
  copula_family = "gumbel",
  base_seed = 1,
  n_cores = NULL,
  alpha_level = 0.05,
  verbose = TRUE,
  augbin_test = c("pooled", "delta"),
  copula_test_type = c("L1", "max")
)
}
\arguments{
\item{M}{Integer. Number of replications.}

\item{B}{Integer. Bootstrap/permutation replicates for the copula
test.}

\item{n_per_arm}{Integer. Patients per arm.}

\item{mean_ctrl, mean_trt}{Numeric vectors of tumour log-ratio means.}

\item{cov_mat}{Numeric \code{T} x \code{T} covariance matrix.}

\item{threshold}{Numeric. Tumour progression threshold. Defaults to 1.2.}

\item{alpha_lesion, beta_lesion, gamma_lesion}{Lesion-model coefficients.}

\item{gamma_lesion_ctrl, gamma_lesion_trt}{Optional per-arm overrides for
the tumour-to-lesion coefficient.}

\item{tumour_dist, tumour_df, lesion_link, lesion_nonlinear}{Optional
model-misspecification arguments passed to
\code{\link{simulate_trial}}.}

\item{copula_family}{Character copula family. Defaults to \code{"gumbel"}.}

\item{base_seed}{Integer.}

\item{n_cores}{Integer or \code{NULL}.}

\item{alpha_level}{Numeric. Rejection threshold. Defaults to 0.05.}

\item{verbose}{Logical.}

\item{augbin_test}{Character. Either \code{"pooled"} (default; uses
\code{\link{augbin_test_pooled}}) or \code{"delta"} (uses
\code{\link{augbin_test_delta}}).}

\item{copula_test_type}{Character. Either \code{"L1"} (default; uses
\code{\link{copula_L1_permutation_test}}) or \code{"max"} (uses
\code{\link{copula_test}}).}
}
\value{
A list with elements \code{power_km}, \code{power_augbin},
  \code{power_copula}, \code{n_failed}, and \code{pvalues}.
}
\description{
Runs \code{M} two-arm replications under one scenario and applies all
three tests (KM log-rank, AugBin pooled-hazard with delta variance,
copula L1 permutation) to each replicate. Returns rejection rates and
the raw matrix of p-values.
}
